//Use the map method to create a new array and divide each number by 2  “num / 2”   from [1, 2, 3, 4, 5].  Log the result.
const arrayBeforeMap = [1, 2, 3, 4, 5];
const arrayAfterMap: number[] = arrayBeforeMap.map((e) => e / 2);
console.log(arrayAfterMap);
