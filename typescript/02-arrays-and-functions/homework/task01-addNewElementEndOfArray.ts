//Create an array of strings and add a new element at the end of the array. Log the result.
const numbers: number[] = [10, 20, 30, 40, 50];
const numberToAdd: number = 60;
numbers.push(numberToAdd);
console.log(numbers);
